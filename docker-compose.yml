version: '3'

services:
  backend:
    build: 
          context: ./server
          dockerfile: Dockerfile
    command: yarn dev
    ports:
      - "3333:3333"
    volumes:
      - ./server:/usr/app
  frontend:
    build:
          context: ./store
          dockerfile: Dockerfile
    command: yarn start
    ports:
      - "3000:3000"
    volumes:
      - ./client:/usr/app
    links:
      - "backend"